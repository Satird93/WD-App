# Быстрый старт

## ⚡ Минимальная настройка для тестирования

### 1. Настройте Supabase

1. Зайдите на https://  supabase.com и создайте новый проект
2. Дождитесь инициализации проекта (1-2 минуты)
3. Перейдите в **Settings** → **API**
4. Скопируйте:
   - **Project URL**
   - **anon/public key**

### 2. Обновите .env файл

Откройте файл `.env` в корне проекта и замените:

```env
VITE_SUPABASE_URL=https://ваш-проект.supabase.co
VITE_SUPABASE_ANON_KEY=ваш_anon_ключ
```

### 3. Примените миграции базы данных

В Supabase Dashboard:

1. Откройте **SQL Editor** (левое меню)
2. Создайте **New query**
3. Скопируйте и выполните файлы миграций **по порядку**:

**Миграция 1** - Базовая схема:
```bash
supabase/migrations/20250119_initial_schema.sql
```

**Миграция 2** - Роли и специализации:
```bash
supabase/migrations/20250119_roles_and_specializations.sql
```

**Миграция 3** - Челленджи:
```bash
supabase/migrations/20250119_extended_challenges.sql
```

**Миграция 4** - Фразы маскота:
```bash
supabase/migrations/20250119_extended_mascot_messages.sql
```

### 4. Перезапустите dev сервер

```bash
# Остановите текущий сервер (Ctrl+C в терминале)
# Запустите заново:
npm run dev
```

### 5. Откройте приложение

Перейдите на http://localhost:3002

**В режиме разработки** приложение автоматически создаст тестового пользователя.

---

## 🎯 Первые действия

### Назначьте себя администратором

1. Откройте приложение в браузере
2. В Supabase перейдите в **Table Editor** → **users**
3. Найдите запись с telegram_id = 123456789 (тестовый пользователь)
4. Нажмите на строку → **Edit**
5. Измените **role** на `admin`
6. Сохраните

### Назначьте специализацию (опционально)

В той же таблице **users** установите **fencing_specialization**:
- `сабля`
- `длинный меч`
- `рапира`

---

## 🧪 Тестирование функций

### Для тренеров/админов:

1. Перейдите на вкладку "Челленджи"
2. Нажмите "+ Создать"
3. Заполните форму и создайте свой челлендж

### Для студентов:

1. Перейдите на вкладку "Челленджи"
2. Выберите категорию (ОФП/Фехтование)
3. Выберите сложность
4. Нажмите "Выполнил" на любом челлендже

---

## 📱 Запуск в Telegram

### Создайте Telegram бота

1. Найдите **@BotFather** в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Получите **Bot Token**

### Настройте Mini App

1. В чате с @BotFather отправьте `/mybots`
2. Выберите вашего бота
3. **Bot Settings** → **Menu Button** → **Configure menu button**
4. Введите URL: `https://ваш-домен.com` (или используйте ngrok для локальной разработки)

### Для локальной разработки с ngrok:

```bash
# Установите ngrok: https://ngrok.com/download
ngrok http 3002
```

Используйте HTTPS URL от ngrok в настройках бота.

---

## 🐛 Частые проблемы

### Ошибка "Откройте приложение через Telegram"

В режиме разработки (`npm run dev`) это не должно происходить. Если видите эту ошибку:
1. Убедитесь, что сервер запущен в dev режиме
2. Проверьте консоль браузера на предупреждение "Development mode: using mock Telegram user"

### Ошибка подключения к Supabase

1. Проверьте `.env` файл - правильные ли URL и ключ
2. Перезапустите dev сервер после изменения `.env`
3. Проверьте, что все миграции применены в Supabase

### Пустой список челленджей

Убедитесь, что применили миграции 3 и 4 (челленджи и фразы маскота).

---

## 📚 Дополнительная информация

Полная документация в [README.md](./README.md)
