# 📋 UPD - Список исправлений для следующего обновления

Дата создания: 2026-02-03

---

## 🔧 Баги для исправления

### 1. Возврат на стартовый экран после выполнения челленджа (только студент)
**Описание:** После выполнения челленджа происходит возврат на стартовый экран выбора направления. Баг проявляется **только у студента**, у тренера такой проблемы нет.
**Ожидаемое поведение:** Оставаться на экране челленджей после выполнения.
**Решение:** Добавлен флаг `setupCompleted` в `App.jsx`, который запоминает, что пользователь уже прошёл/пропустил экран настройки специализации. Теперь после выполнения челленджа пользователь не возвращается на стартовый экран.
**Статус:** ✅ Готово

---

### 2. Кнопка "Пропустить" не работает на стартовом экране
**Описание:** На стартовом экране выбора направления не работает кнопка "Пропустить".
**Ожидаемое поведение:** Кнопка должна пропускать выбор направления и переходить дальше.
**Решение:** Баг был связан с багом #1 — кнопка технически работала, но экран появлялся снова из-за того, что `fencing_specialization` оставался `null`. Исправление бага #1 (добавление флага `setupCompleted`) решило и эту проблему.
**Статус:** ✅ Готово

---

### 3. Не идёт зачёт стриков
**Описание:** Стрики не засчитываются/не обновляются. В один момент они обновились, но потом перестали работать.
**Ожидаемое поведение:** При выполнении условий стрик должен увеличиваться и сохраняться, если последний челлендж был сегодня или вчера.
**Корневая причина:** Файл `REBUILD_DATABASE.sql` содержал старую, багованную версию функции `calculate_streak`, которая требовала выполнения челленджа именно СЕГОДНЯ. Когда база пересоздавалась из этого файла, исправление из миграции `20250203_fix_streak_calculation.sql` терялось.
**Решение:** 
- Исправлена SQL функция `calculate_streak` в обоих файлах: `REBUILD_DATABASE.sql` и миграции `20250203_fix_streak_calculation.sql`
- Новая логика:
  - Стрик сохраняется, если последний челлендж был сегодня или вчера
  - Считаются последовательные дни выполнения челленджей
  - Если последний челлендж был позавчера или раньше - стрик сбрасывается в 0
**Статус:** ✅ Готово (требуется применить миграцию `20250203_fix_streak_calculation.sql` в Supabase)

---

### 4. Добавить возможность удаления аккаунта
**Описание:** Пользователь не может удалить свой аккаунт из приложения.
**Ожидаемое поведение:** Должна быть кнопка/опция для полного удаления аккаунта, которая:
- Удаляет все данные пользователя из базы данных (профиль, достижения, выполненные челленджи, стрики и т.д.)
- Отвязывает `telegram_id`
- Очищает локальные данные сессии
- Возвращает на стартовый экран авторизации
- Требует подтверждения от пользователя перед удалением
**Решение:**
- Добавлена кнопка "Удалить аккаунт" внизу экрана профиля (`Profile.jsx`)
- Реализован диалог подтверждения через Telegram API
- Создана функция `deleteUserAccount` в `telegramAuth.js`
- Благодаря `ON DELETE CASCADE` в БД автоматически удаляются связанные данные
- После удаления приложение перезагружается для повторной авторизации
- Создана миграция: `20250214_account_deletion_rls.sql`
**Статус:** ✅ Готово (требуется применить миграцию в Supabase и протестировать)

---

### 5. Неправильное отображение количества челленджей при фильтрации по сложности
**Описание:** При выборе категории сложности (например, "Хард") счётчики для других категорий показывают 0 вместо реальных значений. На скриншоте видно, что при выбранном "Хард 11" показывается "Легко 0" и "Средне 0", хотя на самом деле их 16 и 22 соответственно.
**Ожидаемое поведение:** Счётчики категорий должны всегда показывать реальное количество челленджей в каждой категории, независимо от того, какая категория сейчас выбрана.
**Решение:** —
**Статус:** ⏳ Ожидает реализации

---

## 📝 Примечания
- Добавляйте новые пункты сюда по мере обнаружения
- После исправления меняйте статус на ✅ Готово
